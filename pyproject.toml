[project]
name = "evolution_whatsapp"
authors = [
    { name = "Yusuf", email = "yusufpaloba43@outlook.com" }
]
description = "Frappe v16 app integrating ERPNext with Evolution API v2 for WhatsApp messaging (shared-number model)"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "evolution_whatsapp"
